# Suppress npm warnings when called internally by Next.js
# pnpm sets environment variables that npm doesn't recognize, causing harmless warnings
# Setting loglevel=error hides these during development
loglevel=error
